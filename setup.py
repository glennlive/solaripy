from setuptools import setup

setup(name='solaripy',
      version='0.1',
      description='simple solar panel tester',
      author='Glenn Richardson',
      packages=['solaripy'],
      zip_safe=False)
